# Infrastructure Workspace

This directory owns local environment helpers and deployment support files.

Current placeholders:

- `infra/docker/` for local Docker Compose and container support
- `infra/scripts/` for local setup or smoke-test helpers
- `infra/.env.example` for local database defaults

Do not place application logic in this directory.
