# Backend Source Layout

This workspace follows the DOWINN backend structure:

- `src/config/` for environment loading and runtime config helpers
- `src/modules/` for domain modules
- `src/common/` for cross-cutting infrastructure
- `src/database/` for persistence helpers

The first implemented module is `health`.
